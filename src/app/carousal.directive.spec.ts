import { CarousalDirective } from './carousal.directive';

describe('CarousalDirective', () => {
  it('should create an instance', () => {
    const directive = new CarousalDirective();
    expect(directive).toBeTruthy();
  });
});
